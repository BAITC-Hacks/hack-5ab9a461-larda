# Прогресс Larda

## 2026-09-23 — единое оформление и доступный каталог студента

- По замечанию пользователя обе роли переведены на общую светлую палитру; ранги, XP и достижения сохранены. Вопрос о предпочтении темы был задан, до ответа использован объявленный вариант общей светлой темы.
- В студенческой навигации «Миссии» переименованы в «Каталог задач»; в профиле добавлена заметная кнопка открытия каталога.
- Добавлены `/student/catalog` и `/student/catalog/:id` на существующих компонентах каталога/карточки, без дублирования данных. `/catalog` и старые ссылки сохранены. Возврат из карточки остаётся в студенческом каталоге.
- Повторно прочитаны доступные backend-файлы. `for_frontend (2).md` отличается заключительным абзацем про папку Postman 03; сохранённая коллекция содержит только 01 (57 запросов) и 02 (3 запроса). Collection (2), указанная во вкладках IDE, на диске не обнаружена. Backend localhost:8080 недоступен; контракт XP/submit/review не изменился. Исходные backend-файлы не редактировались.
- Обновлены BACKEND_INTEGRATION, README, FRONTEND и contextn.
- Проверки: `npm run build` успешно; `npm run test:e2e` — **24 passed, 24.1 s**. Два новых browser-теста проверяют одинаковые цвета ролей, явный вход в каталог, фильтрацию, предложение, refresh, назад/вперёд и возврат в профиль при 1280/390 px.
- Визуально просмотрены новый светлый профиль на 1280 px и каталог на 1280/390 px; переполнения не выявлены. Тестовый сервер завершён штатно; порт 5173 не занимался.

## 2026-09-23 — геймификация и полный цикл исполнения

Статус: **реализовано и проверено локально**, ветка `feat/gamification-mvp`. До начала изменений рабочее дерево было чистым на `feat/business-workspace`. Появившиеся во время работы файлы пользователя в `docs/from backend` не изменялись.

### Что реализовано

- Семь категорий готовности, диапазоны 0–39/40–69/70–89/90–100, рекомендации с доступными баллами и реальным изменением оценки после подтверждения. Низкий рейтинг не блокирует публикацию; черновики приватны.
- Фильтры каталога и синтетические карточки разной полноты. Пять карточек, команд и откликов; read-only пример профиля отделён от свежего студента.
- Отдельный старт после выбора команды: три этапа, критерии, сроки, snapshot условий и состава. Капитан передаёт URL, описание и вклад участников. Бизнес принимает отдельных участников или запрашивает доработку; версии и решения сохраняются.
- Атомарное принятие и начисление XP; защита от повторов, три достижения, уровни и ранги с порогами завершённых проектов. Навыки выводятся из подтверждённых вкладов.
- Бизнес-очередь проверок, просрочек, откликов и проектов. Студенческий тёмный профиль, миссии, награды и портфолио.
- Миграция sessionStorage v1 → v2 без удаления исходного ключа и без вымышленного опыта. Валидируемая локальная заглушка помощника с fallback.
- Актуализированы README, contextn, agent и frontend-документация; добавлен docs/GAMIFICATION.md.

### Фактические проверки

- `npm run build` — успешно (Vite 8.3.0).
- Последний `npm run test:e2e` — **22 passed, 21.7 s**: 14 доменных/repository и 8 браузерных сценариев.
- Проверены веса и границы готовности, уровни/ранги, получатели XP, достижения, последовательность этапов, неизменяемость после старта, права, повтор принятия, отказ записи и восстановление после refresh, миграция.
- Сквозной браузерный цикл проходит создание → подтверждение → публикацию → отклик → выбор; отдельный delivery-сценарий проходит настройку → доработку → повторную версию → все три принятия → 900 XP первого проекта → достижение → портфолио.
- Бизнес получает те же счётчики принятых этапов, студент не получает наград за неподтверждённых участников. Пример профиля не меняет сохранённый workspace.
- Визуально просмотрены скриншоты бизнеса/студента на 1280 px, профиля и проекта на 390 px. Проверены keyboard focus, reduced motion, отсутствие горизонтального переполнения. Исправлены цвет текста основной кнопки и motion-токены тёмной темы.
- Браузерная проверка выявила и помогла исправить сохранение опубликованной карточки: metadata Task больше не обрабатывается как текст Card; добавлен regression test.
- `git diff --check` — успешно. Временный сервер тестов использовал 4175; после прерывания пользователя оставшийся собственный Vite остановлен, финальный прогон завершил сервер штатно. Порт 5173 не занимался.

### Полученные документы backend

Прочитаны frontend guide, JSON reference, Postman collection/environment. Составлен `docs/BACKEND_INTEGRATION.md` с источниками, запуском и матрицей несовместимостей. Живой `localhost:8080/healthz` недоступен; backend-исходников, Compose и упомянутого OpenAPI в checkout нет. Коллекция с изменениями БД не запускалась.

Документированный backend иначе распределяет EXP, начинает работу при принятии отклика и не сохраняет сроки, комментарии, индивидуальный вклад или версии в присланных JSON-моделях. API-интеграция не выполнена: действующий MVP остаётся локальным. Реальная авторизация, серверная верификация, внешний AI, отмена/переоткрытие, completion rate и численные skill levels вне реализации.

---

Ниже — исторический отчёт исходной пересборки; его границы не описывают новые возможности выше.

## 2026-09-23 — пересборка frontend, работа возобновлена

Статус: **реализовано и проверено**. Предыдущие попытки выполнения прерывались. При начале этого этапа проверены файлы и ветка `feat/interactive-mvp-frontend`: исходный прототип находился в `src/App.tsx` и переключал экраны через React state. В ходе этапа он заменён модульным интерфейсом с реальными URL.

### Выполнено и подтверждено

- Прочитаны исходные `README.md`, `package.json`, `src/App.tsx`, проверено состояние Git.
- Созданы `agent.md` — правила разработки; `contextn.md` — продукт, архитектура и полученная ERD; `progress.md` — журнал фактического состояния.
- Добавлен `AGENTS.md` со ссылкой на эти документы для последующих агентов.
- Получена ERD пользователя; зафиксированы задача с владельцем-пользователем, строковые идентификаторы и разделение статусов публикации/исполнения.

### Разделение выполненной работы

- Основной агент: модульные страницы, роутинг, локальный репозиторий и новый кабинет.
- Агент анимаций: общие motion-компоненты и reduced motion.
- Агент ERD: анализ связей, ограничений и соответствия frontend.
- Агент документации: эти документы и финальное обновление по результатам проверок.

### Результат реализации

- Проверено наличие модулей `app`, `domain`, `data`, feature-страниц и общих UI/motion-компонентов; маршруты подключены через `react-router-dom`.
- Реализован `WorkspaceRepository` с локальным адаптером `sessionStorage` (`larda.workspace.v1`), сохранением подтверждённых действий после обновления страницы и проверками публикации/отклика/выбора команды. Несохранённый текст форм не гарантируется при refresh или переходе.
- Правила готовности соответствуют показанному в интерфейсе заполнению: 25 → 45 → 60 → 75, дополнительные поля до 100.
- Черновые факты подтверждаются при публикации; выбор команды выводится из принятого отклика отдельно от статусов публикации и исполнения.
- Добавлен анализ ERD в `docs/ERD_REVIEW.md` (агент ERD).
- В `package.json` добавлен `test:e2e`, сценарии находятся в `tests/workspace.spec.ts`; установлен Chromium build 1243 для Playwright.
- Создана практическая инструкция `docs/FRONTEND.md`: команды, сценарий демо, маршруты, добавление модуля и подключение адаптера backend.
- Пользователь параллельно редактировал `README.md` и `NEW_README.md`; эти изменения сохранены, агент документации их не изменял.

### Проверки

- `npm run build` — успешно (Vite 8.3), по фактическому результату основного агента.
- `npm run test:e2e` — **5 passed**, последний прогон 13,4 с. Сквозной тест также проходит через общий список задач с откликами.
- Пройден полный сценарий: создание → три ответа → редактирование до 85 баллов → публикация → отправка настоящего ввода отклика → ручной выбор; проверено сохранение после refresh.
- Пройдены прямые маршруты, разные страницы задач, браузерные назад/вперёд и недоступный черновик.
- Проверено ограничение одной выбранной команды; остальные отклики остаются pending до отдельного ручного отклонения.
- Проверены reduced motion, доступ с клавиатуры и отсутствие горизонтального переполнения на ширине 390 px.
- Проверены запрет публикации незавершённой задачи и сохранение введённых пользователем фактов без вымышленных данных.
- Основной агент визуально просмотрел скриншоты выбора роли, рабочего стола, помощника и выбора команды на ширине 1280 px, а также рабочего стола на ширине 390 px. Исправлено скрытие ссылки пропуска навигации; снимки дождались окончания переходов и обновления чисел.
- Общая кнопка «Отклики команд» открывает `/business/tasks?view=responses`: видны все свои задачи с ответами, а не только первая. Проверено откликами на две разные задачи.
- `git diff --check` для изменённых нами файлов проходит. README/NEW_README пользователя не исправлялись и не форматировались.
- Тестовый сервер на `127.0.0.1:4175` автоматически завершён после тестов; порт 5173 тестами не занимался.

### Границы этапа

Backend, авторизация, реальный AI, EXP, достижения и выполнение этапов работ не подключены. HTTP-контракт нужно согласовать перед заменой локального адаптера. Текущая пересборка frontend завершена в согласованных границах; результаты проверок выше получены от основного агента, который запускал сборку и браузерные тесты.

## 2026-09-23 — заявки студента и подключаемый серверный сценарий

- Найдена причина недоступности новой заявки в трёх стартовых карточках: там уже есть синтетические заявки текущей команды. Каталог не показывал собственный статус и создавал впечатление, что заявку ещё можно отправить. Добавлены статусы, фильтр «Только доступные для моей заявки» и список заявок в профиле. Повторные локальные заявки не разблокированы обходом правил.
- На `/connected` добавлен отдельный серверный кабинет; вход с выбора роли. HTTP-клиент получает реальные ID пользователей/команд, передаёт demo-header, обрабатывает ошибки/204, отмену и 20-секундный timeout. Основной локальный WorkspaceRepository сохранён.
- Серверный кабинет: создание, AI polling до 3 минут, пакет ответов, PATCH изменённых полей, подтверждение revision и публикация; заявка капитана, выбор бизнеса, создание/согласование этапа, сдача ссылки, доработка/принятие, явное завершение proposal/task, серверный профиль/XP. Нет внешнего ключа AI во frontend, локальные награды не добавляются к серверным.
- Проверено: `npm run build` успешно; `npm run test:e2e` — **28 passed (23.0 s)**. Четыре новых теста покрывают фильтр существующих заявок, недоступный сервер, серверную доставку с доработкой и AI → публикацию. Серверные тесты используют Playwright HTTP interception, а не настоящий backend. Старые локальные сквозные проверки также проходят.
- Визуально просмотрен новый кабинет на 390 и 1280 px, мобильное переполнение проверено автоматически; reduced motion включён в AI-сценарии. `git diff --check` проходит. Тестовый сервер 4175 завершён.
- **Блокер живой интеграции:** повторный `curl http://localhost:8080/healthz` — connection refused. Пользователю запрошен URL/путь backend. Модель AI и серверная БД не проверены. Присланные исходные документы не изменялись.
- Границы: серверный кабинет отдельный от локальных dashboard; соединение и выбранная карточка не переживают refresh, но повторное подключение читает серверные записи; каталог ограничен 100 задачами; команды обновляются при переподключении. Нет заявления о полной замене локальной архитектуры или проверенном production.

## 2026-09-23 — merge main с backend

- Получен актуальный `origin/main` (`b448574`), создан merge-коммит `ff3cd8c` в `feat/gamification-mvp` (родители `1f35720`, `b448574`). Backend-код и контракт вошли без изменений.
- Конфликты `.gitignore` и README разрешены: объединены правила исключений, frontend README сохранён, полный backend README вынесен в корневой `BACKEND.md` с сохранением относительных ссылок.
- Пять незакоммиченных файлов перед merge сохранены в stash и восстановлены. До обновления документации подтверждено полное совпадение восстановленных файлов со stash. Резервный stash `safety-before-main-merge-gamification-2026-09-23` оставлен. Документация в рабочем дереве дополнена состоянием после merge.
- `npm run build`, `go test ./...`, `go vet ./...` успешны. PostgreSQL integration tests пропущены без `TEST_DATABASE_URL`; БД и сервер не запускались.
- Первый браузерный прогон параллельно с компиляцией Go: 27 passed, 1 timeout (30 секунд). Повторный отдельный `npm run test:e2e`: **28 passed (24.3 s)** без изменений кода или увеличения таймаутов.
- Конфликтов не осталось, `git diff --check` проходит. Push не выполнялся.

## 2026-09-23 — реальное объединение frontend и backend

### Реализовано

- Основной `App` теперь использует API и PostgreSQL; fixture repository включается только через `VITE_DATA_MODE=local` / `npm run dev:local`. При ошибке API показана ошибка, подмены серверных данных демо-данными нет.
- Автоматическое подключение к своему origin. Vite dev/preview proxy `/api` и `/healthz` → 8080; `Dockerfile.frontend`, nginx и сервис `web` в Compose дают единый адрес на 3000. Обновлены разрешённые локальные CORS origins.
- Общий серверный кабинет бизнеса и студента: реальные пользователи/команды, каталог/поиск, заявки капитана, решения бизнеса, AI-цикл, публикация, этапы, доработка, принятие, завершение, профиль/EXP/серверные достижения. Бизнес получает очередь результатов и предложений; студент — уровень по серверному XP, этапы и ссылки на завершённые командные результаты. Навыки профиля не выдаются за индивидуально верифицированные.
- Профиль и задача хранятся в URL. Проверены refresh, назад/вперёд, прямые ссылки каталога и смена роли с приватной карточки. Членство обновляется при загрузке. Галочка подтверждения привязана к task/revision; исправлена гонка между AI polling и повторной загрузкой карточки.
- Старые локальные RPG-правила не подменяют серверные. Ранги и персональный вклад не объявлены реализованными backend-возможностями. Инструкции и ограничения — `docs/RUN_INTEGRATED.md`.

### Фактическая проверка

- Docker daemon недоступен пользователю; через rootless Podman поднят PostgreSQL 16 (`larda-integration-db`, 55432, постоянный volume). Нативный Go API запущен на 127.0.0.1:8080 с явным `AI_MODE=fallback`.
- `npm run build` — успешно. Последний `npm run test:e2e` — **28 passed (23.0 s)**.
- `npm run test:live` — **3 passed (14.5 s)**. Полный сценарий прошёл через настоящую БД и Go API, без HTTP-подмены: создание → вопросы → ответы → confirm/publish → заявка → выбор → этап → возврат → повторная сдача → принятие → завершение. Подтверждены отсутствие XP за заявку/отклонённый результат, отсутствие повторного начисления, refresh и история. Отдельно проверена искусственная ошибка bootstrap (503) без fallback на fixtures и переход из приватного draft URL в роль студента.
- Первый live-прогон после изменения подтверждения выявил сброс checkbox во время завершения polling; ошибка исправлена, последующие прогоны успешны. Это исправление frontend, не увеличение таймаута теста.
- `TEST_DATABASE_URL=... go test ./...` — успешно, включая PostgreSQL integration tests (первый реальный прогон postgres 5.480 s); `go vet ./...` — успешно.
- Frontend container image собран через Podman; Compose-конфигурация валидна; nginx configuration test успешен в отдельном сетевом namespace. Попытка nginx test с host network упёрлась в права на порт 80; проверка повторена без host network. Полный Docker Compose runtime не запускался из-за доступа к daemon.
- Просмотрены desktop/mobile screenshots студента, карточки и очереди бизнеса; проверены 1280/390 px, отсутствие мобильного overflow, reduced motion. `git diff --check` — успешно.

### Доступный результат и границы

- Оставлен рабочий preview: **http://127.0.0.1:3000**, API 8080, PostgreSQL 55432. `/healthz` через frontend возвращает `status: ok`; прямой URL каталога отдаёт приложение. Порт 5173 не занимался. Временный тестовый сервер 4175 завершён.
- Live-тесты создали явно подписанные проверочные задачи и результаты в отдельной локальной БД. Серверный `fallback` обрабатывает задания и очередь, но не вызывает внешнюю модель. Проверка OpenAI и production-auth не выполнены.
- Сохранены прежние незакоммиченные изменения; работа остаётся в `feat/gamification-mvp`. Commit/push этого этапа не выполнялись.
